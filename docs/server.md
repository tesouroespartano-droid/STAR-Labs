# Server

Ainda não há servidor. O contrato planejado separa autenticação, usuários, dispositivos, chaves, planos, entitlements, sessões, scripts, versões, flags, auditoria, administração e health. Respostas usarão `{data:{}}` ou `{error:{code,message}}`.