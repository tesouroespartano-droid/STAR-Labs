# Compatibilidade GS2

Status atual: parcial e experimental. Suportados no harness: literals, variáveis locais, funções, chamadas, propriedades, objetos, operadores básicos, `if`, `while` e `return`. Eventos, arrays, timers, sintaxe GS2 real e built-ins de cliente ainda precisam de especificação e fixtures autorizadas.