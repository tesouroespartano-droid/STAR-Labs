# Hooks

Hooks STAR Labs devem identificar alvo, função e callback, preservar chamada original, ordenar a cadeia, impedir recursão acidental e devolver um handle de unhook. O código atual contém apenas um registro simplificado por evento.