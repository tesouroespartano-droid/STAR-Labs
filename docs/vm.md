# VM

A VM inicial usa frames lexicais, funções, literals, objetos, propriedades, chamadas, condicionais, loops e quota de execução. Bytecode explícito ainda é uma etapa pendente; o AST não deve ser tratado como formato final.