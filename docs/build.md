# Build

Comandos raiz planejados: `analyze`, `test`, `lint`, `build-runtime`, `build-android`, `package-apk` e `build-apk`. O pipeline deve falhar quando Android SDK, projeto, dependências ou assinatura própria estiverem ausentes.